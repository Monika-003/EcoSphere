services:
  - type: web
    name: ecosphere
    env: static
    staticPublishPath: ./frontend
    buildCommand: echo "static site — no build needed"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
